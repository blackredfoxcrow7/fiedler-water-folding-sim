# 🧬 水共役・トポロジカル核形成伝播モデル（Model ⑨）アルゴリズム技術解説

本ドキュメントは、水共役・トポロジカル核形成伝播モデル（[server_overall_fiedler_nucleation.py](file:///home/eldenring/newProject/server_overall_fiedler_nucleation.py)）の内部アルゴリズム、角度決定プロセス、Fiedler値の計算タイミング、および結合のロック（凍結）メカニズムについて詳細に解説する技術仕様書です。

---

## 1. 全体アルゴリズムの概要

本モデルは、**「確率的探索空間の決定論的拘束による縮小」**を数理トポロジー的に実現したフォールディング・エンジンです。
ペプチドが持つ「親水・疎水マップ（アミノ酸配列情報）」を、周囲の水分子集団（溶媒）が力学緩和を介して「水素結合ネットワーク（トポロジー）」へと翻訳します。そして、形成された強固な局所相互作用（核）を恒久的にグラフにロックし、下流のジョイントの回転自由度を決定論的に縛りながら、ミリメートルレベルの精度で天然構造へと収束させます。

---

## 2. 角度はどのようにして決まるのか（決定ステップ）

シミュレーションが始まると、アミノ酸の主鎖 of 二面角（$\phi$, $\psi$ ジョイント）を順番にスイープし、以下のプロセスで各ジョイントの角度を決定します。

### 【ステップ 1】候補角度の仮回転
特定の回転ジョイントについて、以下の4つの候補角度（回転デルタ）を生成します。
$$\Delta \theta \in \{-0.25, -0.05, 0.05, 0.25\} \text{ （ラジアン）}$$
候補角度ごとに、ペプチドのダウンストリーム原子の三次元座標を仮に回転させます（`rotate_joint`）。

### 【ステップ 2】溶媒（水分子）のリアルタイム追従
ペプチド原子の移動に合わせて、周囲の全水分子（186個〜308個）の空間配置を **2ステップ** 力学緩和させます（`update_solvent_physics`）。
* 親水アミノ酸の原子（N, O）は水を**引き寄せ**、疎水アミノ酸の炭素（C）は水を**排除（反発）**します。
* 水分子同士は適切な距離（約 $2.8\text{ \AA}$）を維持するように押し合います。
* これにより、各仮配座における最適な「仮の水和配置」が作られます。

### 【ステップ 3】動的共役グラフ $G_{cand}$ の仮構築
ペプチドと緩和後の水分子の位置から、仮の接続関係グラフ $G_{cand}$ を再構築します。
1. **ペプチド共有結合**: 重み $10.0$ で接続（固定）。
2. **これまでにロックされたコンタクト（`locked_contacts`）**: **現在の距離 $d$** を用いて、動的な重み $W(d) = C / d^2$ を計算して接続（詳細は第4節）。
3. **新規の仮コンタクト**: 距離が閾値（親水 $4.0\text{ \AA}$、疎水 $4.5\text{ \AA}$）未満の原子間を接続。
4. **水 - 水、水 - ペプチド結合**: 距離が閾値（水-水 $3.5\text{ \AA}$、水-ペプ $3.0\text{ \AA}$）未満の近傍最大4接続を接続。

### 【ステップ 4】統合Fiedler値の評価と決定
構築された $G_{cand}$ 全体のFiedler値（接続効率）を計算します。
4つの角度候補の中から、**「統合Fiedler値が最も高くなった（＝水分子とペプチド全体が最も綺麗に結びついた）角度」**を選択し、座標を正式にアップデートします。

---

## 3. Fiedler値の計算とフィードバックのタイミング

Fiedler値は、シミュレーション中、以下の **2つのタイミング** で計算され、即座にフィードバックされます。

### ① 【探索・評価タイミング】仮回転ループの中（毎秒数百回）
* **タイミング**: 各ジョイントの4つの候補角度を評価するループの内部。
* **目的**: どの角度がトポロジー的に最適かを比較・選択するため。
* **計算方法**: 
  仮構築されたグラフ $G_{cand}$ のラプラシアン行列 $L = D - A$（$D$ は次数行列、$A$ は隣接行列）を構築し、その固有値スペクトルを求めます。
  $$\text{spectrum} = \{\lambda_1, \lambda_2, \lambda_3, \dots, \lambda_n\} \quad (0 = \lambda_1 \le \lambda_2 \le \lambda_3 \dots)$$
  この中の **第2最小固有値 $\lambda_2$（Fiedler値）** を抽出します。これが探索の「スコア」となり、最も $\lambda_2$ が大きい角度が選択されます。

### ② 【状態確定・保存タイミング】ジョイント確定の直後
* **タイミング**: 最良の角度が選ばれ、ペプチドと水分子の三次元座標が実際に確定した直後。
* **目的**: 現在確定した立体構造から、どの相互作用が「新しくロック（凍結）されるべきか」を判定するため。
* **計算・フィードバック**: 
  確定した座標を元に再度グラフを評価し、次に述べる条件を満たす結合を `locked_contacts` リストに恒久登録します。これにより、次のジョイントの計算からは、この結合が「壊してはならない拘束」としてFiedler計算にフィードバックされます。

---

## 4. どの結合が残される（ロックされる）のか

ペプチドが回転し、ステップが確定するたびに、ペプチド内部のアミノ酸間で以下の条件を満たす非共有結合が **`locked_contacts` セットに恒久的に追加（ロック）** されます。

### 【ロック対象となる結合種と距離閾値】
1. **主鎖同士の水素結合（Polar Contacts）**: 
   アミノ酸の主鎖原子（N, O）同士の距離 $d$ が **$4.0\text{ \AA}$ 未満** のとき。
2. **疎水性側鎖のパッキング（Hydrophobic Contacts）**: 
   疎水性アミノ酸の側鎖炭素（C）同士の距離 $d$ が **$4.5\text{ \AA}$ 未満** のとき。
3. **主鎖 - 側鎖間の極性結合（BB-SC Polar Contacts）**: 
   主鎖と側鎖の親水性原子間の距離 $d$ が **$6.5\text{ \AA}$ 未満** のとき。

※水分子同士（水-水）、および水とペプチドの間（水-ペプ）の結合は、流動性を維持するため**絶対にロックされません**。

---

## 5. ロックされた結合が「拘束条件」として働く数理トリック

一度 `locked_contacts` に登録された結合は、グラフから消えずに残り続けますが、その接続の強さ（重み）は固定されず、**現在の原子間距離 $d$ に応じて動的に変動**します。

* **親水性結合の動的重み**: $W(d) = 16.0 / d^2$
* **疎水性結合の動的重み**: $W(d) = 40.0 / d^2$

### 拘束の伝播プロセス
1. ある局所的なターンで水素結合が形成され、距離が $2.8\text{ \AA}$ になってロックされたとします。このときのエッジの重みは $W \approx 16.0 / (2.8)^2 \approx 2.04$ です。
2. 次のステップで、別のジョイントがこの結合を引き剥がす（例：距離 $d$ を $6.0\text{ \AA}$ に広げる）ように回転しようとすると、そのロック結合の重みは $W \approx 16.0 / (6.0)^2 \approx 0.44$ に急減します。
3. グラフ理論において、一部のエッジの重みが急減すると、グラフ全体の接続度を表す **Fiedler値（$\lambda_2$）が著しく低下** します。
4. 結果として、Fiedler値を最大化しようとするアルゴリズムは、この「既存のロック結合を引き伸ばす回転」を自動的に却下（不採用）にし、**「すでに結合した部分を壊さないような回転」だけを自動的に選択**するようになります。

これが、**「局所的な核（すでに形成された結合）が、その後に続く配列の配置を決定論的に制限してゆく」** という、物理的な自己組織化のトポロジカルな実装メカニズムです。
